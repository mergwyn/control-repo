---

classes:
  - network

dhcp::failover::role:         primary
dhcp::failover::peer_address: 192.168.11.22

network::interfaces_hash:
  eth0:
    method:                     static
    pre_up:                     
      - /sbin/ifconfig eth0 mtu 9004
    ipaddress:                  "192.168.11.21"
    netmask:                    "%{hiera('defaults::netmask')}"
    gateway:                    "%{hiera('defaults::gateway')}"
    network:                    "%{hiera('defaults::network')}"
    dns_nameservers:            "192.168.11.21 192.168.11.22"
    dns_search:                 "%{hiera('defaults::dns_search')}"

# vim: sw=2:ai:nu expandtab
