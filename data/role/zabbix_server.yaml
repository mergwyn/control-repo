#
---

classes:
  - profile::base  # All roles should have the base profile
  - profile::domain::sso
  - profile::zabbix::server

zabbix::agent::server:       "127.0.0.1,%{trusted.hostname},%{trusted.certname},localhost"
zabbix::agent::serveractive: "127.0.0.1,%{trusted.hostname},%{trusted.certname},localhost"

# vim: sw=2:ai:nu expandtab
