#
---

classes:
  - profile::base  # All roles should have the base profile
  - profile::domain::sso
  - profile::zabbix::server

zabbix::agent::server:       "%{trusted.hostname},%{trusted.certname},localhost"
zabbix::agent::serveractive: "%{trusted.hostname},%{trusted.certname},localhost"

# vim: sw=2:ai:nu expandtab
