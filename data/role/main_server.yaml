#
---

classes:
  - profile::base  # All roles should have the base profile
  - profile::domain::sso
  - profile::nfs_server
  - profile::web::webdav
  - profile::web::munki
  - profile::photos
  - profile::mac::timemachine_server
  - profile::mailman_rss
  - profile::virtual::lxd
  - profile::backuppc::server

backuppc::server::backuppc_password: "%{hiera('secrets::backuppc')}"
backuppc::serveR::gzip_path:         '/usr/bin/pigz'
backuppc::server::full_keep_count:
  - 4
  - 0
  - 12


# vim: sw=2:ai:nu expandtab
